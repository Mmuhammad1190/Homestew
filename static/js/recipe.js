//Recipe.html

const recipeText = $('.recipe_summary')[0];
const saveRecipe = $('.recipe_save')

//Change the recipe summary to text instead of html returned 
if (recipeText) {
    recipeText.innerHTML = recipeText.innerText;
}








